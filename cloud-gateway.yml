zuul:
  routes:
    portfolio: /api/users**

portfolio:
  ribbon:
    ReadTimeout: 60000

hystrix:
  command:
    portfolio:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 60000

ribbon:
  eureka:
    enabled: false