zuul:
  routes:
    portfolio: /api/users**
portfolio:
  ribbon:
    ReadTimeout: 60000
    
ribbon:
  eureka:
    enabled: false

hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 60000